import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Traders Launch — 1-step prop firm, code JTNQ · JackTradesNQ',
  description: 'Traders Launch: daily payouts, one-step funding, no consistency rules, up to $300K. 15% off with code JTNQ.',
};

export default function TradersLaunchLayout({ children }: { children: React.ReactNode }) {
  return children;
}
