import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Top One Futures — 90/10 prop firm, code JTNQ · JackTradesNQ',
  description: 'Top One Futures: 90/10 split, fast payouts, one-step & instant funding up to $150K. Get funded with code JTNQ.',
};

export default function TopOneLayout({ children }: { children: React.ReactNode }) {
  return children;
}
